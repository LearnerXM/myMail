dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${my.mail.nacos.server.url}
    parameters:
      namespace: ${my.mail.dubbo.nacos.namespace}
      group: ${my.mail.dubbo.nacos.group}
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false